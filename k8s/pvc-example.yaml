# Legacy/example PVC manifest.
# Prefer the Helm chart PVC unless you are applying resources manually.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: consul-change-logger-outbox
  namespace: consul
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
