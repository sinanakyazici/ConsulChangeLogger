# Legacy/example Service patch.
# The Helm chart NOTES.txt prints the preferred patch snippet for the existing browser-facing Service.
apiVersion: v1
kind: Service
metadata:
  name: consul-ui
  namespace: consul
spec:
  selector:
    app: consul
  ports:
    - name: http
      port: 80
      targetPort: logger-http
